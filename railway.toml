
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run railway:start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Volume configuration
# Note: Volumes must be created in the Railway dashboard and mounted to /data
# The application will use /data/uploads for file storage
