# Database Configuration
# Railway will provide this automatically when you add a PostgreSQL service
DATABASE_URL="postgresql://username:password@host:port/database"

# NextAuth Configuration
# Generate a random secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="https://your-app.railway.app"

# Local File Storage Configuration (for document storage)
# This directory will be mounted to Railway's persistent volume
UPLOAD_DIR=/data/uploads

# Azure Document Intelligence (for document processing)
AZURE_DOC_INTELLIGENCE_API_KEY=your-azure-api-key
AZURE_DOC_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/

# Node Environment
NODE_ENV=production

# Optional: Custom Next.js Configuration
# NEXT_DIST_DIR=.next
# NEXT_OUTPUT_MODE=standalone
